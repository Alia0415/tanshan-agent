# 框架验收状态

2026-09-12：范围已改为知乎站内通用 Agent。以下为本地受控验收，不代表已经接入知乎站内或通过真实账号 API 验收。

已完成：24 项受控测试、ESLint、TypeScript、生产构建和 HTTP smoke。浏览器实际走通「如何评价远程办公 → 比较与选择 → 场景与关注点 → 演示回答」，当前 633px 视口未出现横向溢出。没有执行真实付费 API 调用。

## 自动验证

`npm run check` 覆盖跨领域追问、明确问题直答、最多两轮、跳过、自由补充进入搜索与生成、条件修改、身份隔离、版本与并发、旧结果隔离、摘要与引用处理、空结果、限流和鉴权错误。额外覆盖全网搜索渠道和三次调用预算，以及纯文字 Agent 完整对话。

`npm run build` 验证 Next.js 生产构建。`npm run test:smoke` 对运行中的 demo 服务验证卡片流程和文字消息 API。当前运行记录见交付说明与命令输出，不将历史大学版浏览器记录当作新版验收。

## 尚未完成

- 站内宿主的创建、鉴权、消息、工具绑定和发布协议尚未提供，因此未完成站内接入。
- 未配置真实知乎凭证；搜索、模型权限、质量、耗时和引用忠实度均待真实联调。
- 意图层是保守规则，复杂否定、隐含目的和跨话题修正不等同于模型理解。未识别信息按原话保存。
- 当前提供完整文本与阶段反馈，没有逐字流式输出。
- 运行时为单进程持久磁盘；正式宿主需要事件去重与适配。没有本人信息、知识库或长期画像的默认访问。
