# 框架验收状态

2026-09-12：范围已改为知乎站内通用 Agent，最终结果只收录知乎帖子。以下分别记录本地受控测试与真实 API 联调，不代表已经接入知乎站内。

此前帖子流程已完成：30 项受控测试、ESLint、TypeScript 与生产构建。浏览器在真实模式提交「目前远程办公的实际体验怎么样？」，返回 5 篇知乎专栏文章，均保留标题、作者、摘要和原始追踪链接。帖子列表位于总结之前；AI 辅助总结默认折叠，展开后显示 5 个分析段落及引用。1280px 视口没有横向溢出。当前使用 auto 生成模式；本次收到结构化结果，但平台也可能返回普通文本，不保证每次提供逐条引用。

历史 demo HTTP smoke 已通过卡片和文字流程。本次没有对真实服务运行 demo smoke，以免将演示断言误用于真实 API。

## 自动验证

动态追问更新（2026-09-12）：已接入 DeepSeek，最多 5 轮且可提前结束。真实接口检查了工作变动、相机、室友、事实题和一次补充：分别生成职业诉求、拍摄用途、作息冲突等追问；相机补充充分后直接回答，事实题不追问。浏览器提交「预算6000元，想买相机，应该怎么选？」后显示具体用途选项与「1 / 5 轮」，选择旅行风景后自动检索并展示 5 篇知乎帖子。

动态追问阶段 44 项受控测试、ESLint、TypeScript 全部通过。新增测试覆盖模型请求与结构校验、历轮问答进入检索/生成、第五轮硬停止、提前结束、跳过、动态选项编号、未知选项拒绝、模型问句不当成用户事实、失败保持原版本、晚返回结果不覆盖编辑、重复问句处理及否定表达。生产构建通过。真实接口请求使用虚构测试问题，不输出本地密钥。

搜索质量更新（2026-09-12）：完整问题、确认条件和历轮补充进入检索规划，生成最多两条互补搜索词，每条最多召回 10 篇。根据原文摘要筛选相关性、识别明确条件冲突；直接相关结果少于 3 篇时最多补搜一次，最终最多显示 5 篇，有足够直接匹配时不再用较弱材料补满数量。筛选理由必须附有能在标题或摘要中找到的原文片段；某条依据不合法时仅跳过该条。模型失败显示降级说明，不把未筛选结果包装成已验证答案。

搜索更新后 `npm run check` 的 60 项受控测试、ESLint、TypeScript 与生产构建通过。新增覆盖互补查询、预算冲突与信息缺失的区分、逐条依据校验、来源编号、查询预算、弱结果补搜、取消与模型故障。真实 API 使用虚构的远程办公和旅行相机场景：首轮均召回 20 篇候选并完成语义筛选。受控的真实模型检查确认排除安防摄像头和明确超预算装备，同时保留缺少价格的有用方法与带价格时效限制的个人经历。预算规则随后收紧，跨预算区间和未含镜头价格不能直接视为满足预算。

可用 `npm run test:search` 显式运行付费联调，原始候选、搜索词和筛选记录写入被 Git 忽略的 `.data/search-quality-check.json`。这些小样本用于检查流程和已知歧义，不构成总体搜索准确率或相对旧版提升比例的评估。

新版生产构建的独立浏览器联调已完成：提交「预算6000元含镜头，想选轻便相机用于徒步旅行拍风景，只拍照片，不拍视频。直接回答」，最终显示 4 篇知乎帖子、AI 筛选说明、相关原文片段及原帖链接。展开总结能看到历史价格与未确认套机预算的限制。测试使用独立端口和数据库，没有替换用户当前提问；主预览已重启加载新版构建。

互动与时间排序更新（2026-09-12）：真实接口确认 `CommentCount`、`VoteUpCount`、`EditTime` 返回数值，现已保留至 Source。在语义相关性与条件匹配度相同的层级内综合互动量和新近程度排序，并在卡片/文字回复显示评论、赞同和更新时间。`npm run check` 的 66 项测试与生产构建通过，覆盖相关性优先、超预算/无关热帖不能补回、三项信号各自有效、极端值递减、缺失/非法数据、前五名截取及引用编号。

独立真实浏览器测试使用远程办公协作与生活平衡问题，返回 5 篇帖子。首篇显示 19 评论、303 赞同、更新于 2022-02-15，后续包含 2025/2026 年的相关经验，数值和排序已在页面核对。这是当次接口快照，不代表当前全站热度；旧帖不会因年份被一律排除，热度也不能保证内容正确。本次页面测试使用 sources 模式，未增加直答生成调用，主预览的生成设置保持原配置。

`npm run check` 覆盖跨领域追问、明确问题直答、最多 5 轮、跳过、自由补充进入搜索与生成、条件修改、身份隔离、版本与并发、旧结果隔离、摘要与引用处理、空结果、限流和鉴权错误。保留本地规则演示的两轮兼容测试。另验证固定知乎搜索接口、帖子类型和 URL 过滤、原始链接保留、三次查询预算、生成前过滤站外资料，以及文字回复中帖子优先和历史站外总结隐藏。

`npm run build` 验证 Next.js 生产构建。`npm run test:smoke` 对运行中的 demo 服务验证卡片流程和文字消息 API。当前运行记录见交付说明与命令输出，不将历史大学版浏览器记录当作新版验收。

## 尚未完成

- 站内宿主的创建、鉴权、消息、工具绑定和发布协议尚未提供，因此未完成站内接入。
- 搜索与直答访问权限已通过真实联调，当前 API 可以生成辅助总结。旧检索曾混有远控工具测评，新流程已增加语义区分与筛选；更广泛问题集的结果相关度和引用语义仍需评估，来源中的个人经历和测评不能视为普遍结论。摘要筛选不能核实原帖之外的现价或事实。
- 追问由模型理解上下文并规划，但相关性与语义去重仍需持续评估。结构化条件仍保守提取，用户原话和问答对应关系完整保存。
- 当前提供完整文本与阶段反馈，没有逐字流式输出。
- 运行时为单进程持久磁盘；正式宿主需要事件去重与适配。没有本人信息、知识库或长期画像的默认访问。
